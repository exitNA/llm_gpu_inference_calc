# 文档模板

## 使用规则

- 先确定主文档，再更新次级文档，最后改代码。
- 同一概念不在多个文档里重复展开。
- 只保留与当前仓库有关的模板，不引入不存在的目录结构。

## 1. 方法说明模板（`docs/llm_gpu_inference_calc.md`）

```markdown
# <主题名称>

## 目标
- 说明本文解决什么问题。

## 适用范围
- 适用：
- 不适用：

## 核心输入
- 参数：
- 单位：
- 默认值：

## 核心公式
- 公式：
- 近似前提：

## 输出结果
- 输出字段：
- 解释：
```

## 2. README 使用说明模板（`README.md`）

```markdown
# gpu-analysis

## 功能
- 能做什么：

## 快速开始
- 运行命令：

## 输入参数
- 关键配置：

## 输出说明
- 结果字段：
```
