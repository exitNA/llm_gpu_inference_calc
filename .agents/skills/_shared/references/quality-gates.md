# 质量门禁（共享）

适用范围：项目内所有 Python 与文档改动类 skills。

## 执行规则

- 至少执行与本次改动直接相关的最小验证。
- 当前仓库没有稳定测试套件时，必须说明缺口，而不是伪造“已验证”。

## 最小门禁矩阵

### Python 计算逻辑改动

- `python -m py_compile main.py`
- `python main.py`

### 文档改动

- 检查所有被引用文件路径真实存在。
- 检查文档中的公式、字段名、默认值与 `main.py` 一致。

### 代码与文档同时改动

- 叠加执行以上两类检查。
